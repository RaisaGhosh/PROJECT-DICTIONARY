import { combineReducers } from "redux";

import posts from "./posts";

const allReducers = combineReducers({
    posts                //as posts : posts , we can just write posts

});

export default allReducers;

